# CSV Parser with Edge Cases

Build a CSV parser that handles: quoted fields, escaped quotes, newlines within quotes, different delimiters, BOM markers, and empty fields. Return structured data.

- Category: feature
- Difficulty: medium
- Verify: `cd . && python3 -m pytest -v`
