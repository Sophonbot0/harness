# Caching Layer

Add a caching layer to an existing data service. Support TTL, LRU eviction, cache invalidation on writes, and cache statistics. Must not change the existing API interface.

- Category: feature
- Difficulty: medium
- Verify: `cd . && python3 -m pytest -v`
