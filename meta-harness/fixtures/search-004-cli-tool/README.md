# CLI Tool with Subcommands

Build a CLI tool with 3 subcommands: 'init' (create config file), 'run' (execute pipeline), 'status' (show current state). Include --help, --verbose flags, and proper exit codes.

- Category: feature
- Difficulty: medium
- Verify: `cd . && python3 -m pytest -v`
