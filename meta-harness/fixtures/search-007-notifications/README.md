# Multi-File Feature with Tests

Add a notification system: create NotificationService, EmailProvider, WebhookProvider, a notification queue, and a retry mechanism. Include unit tests for each component and an integration test.

- Category: feature
- Difficulty: hard
- Verify: `cd . && python3 -m pytest -v`
