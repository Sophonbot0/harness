# Error Handling Overhaul

Replace all bare try/except blocks with proper error handling: custom exception classes, structured error responses, error logging with context, and a global error handler.

- Category: refactor
- Difficulty: medium
- Verify: `cd . && python3 -m pytest -v`
