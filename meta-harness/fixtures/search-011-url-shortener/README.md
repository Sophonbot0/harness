# Sprint-Sized Project

Build a URL shortener service: URL creation API, redirect handler, click analytics tracking, rate limiting, admin dashboard (list all URLs, see click counts), and database persistence. Must have tests.

- Category: project
- Difficulty: hard
- Verify: `cd . && python3 -m pytest -v`
