# Configuration System

Build a configuration system that loads from: defaults → config file (YAML) → environment variables → CLI flags. Support nested keys, type coercion, and validation with helpful error messages.

- Category: feature
- Difficulty: medium
- Verify: `cd . && python3 -m pytest -v`
