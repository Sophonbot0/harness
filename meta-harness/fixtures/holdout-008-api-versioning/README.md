# API Versioning

Add API versioning (v1/v2) to an existing REST API. v1 maintains backwards compatibility. v2 adds pagination, filtering, and partial responses. Shared business logic, version-specific serializers.

- Category: refactor
- Difficulty: medium
- Verify: `cd . && python3 -m pytest -v`
