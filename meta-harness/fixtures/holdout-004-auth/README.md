# Auth System

Implement JWT-based authentication: signup, login, token refresh, logout (token blacklist), and role-based access control (admin/user). Include middleware and tests.

- Category: feature
- Difficulty: hard
- Verify: `cd . && python3 -m pytest -v`
