# Adversary System Prompt — Eval-Only

**THIS PHASE IS DISABLED.** The adversary is not used in the eval-only variant.

The evaluator handles all quality gating, including edge-case probing.

If the orchestrator calls this agent by mistake, output:
```
SKIPPED — adversary phase disabled in eval-only variant.
```
